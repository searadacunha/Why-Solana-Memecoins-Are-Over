# allow_identity.txt -- identifiers that are PUBLISHED ON PURPOSE.
#
# check_no_secrets.py treats these as intentional attribution, not leaks: a
# finding whose fragment matches one of these (case-insensitive) is reported as
# "allowed", not as a failure. This is the deliberate counterpart to the
# --identity denylist: the author signs this work, so the name and the trading
# handle that appears on the committed screenshots are meant to be here.
#
# Keep this SHORT and specific. It is not a place to silence real leaks -- it is
# the record of which identity strings are load-bearing to the dossier's
# author-attribution claim (see the "Author" section of README.md).
#
# One string per line; blank lines and #-comments ignored.

teamdacunha
benjamindacunha
benjamin da cunha
# the author's KYC'd exchange deposit address, published since 2026-08
# (README.md, "Author"; measured by code/expl_ledger.py)
6tmiM84AxMzmXzRByq7m1dgNkHtn9wp671e1GMe2ZmWU
